# Airflow Pipelines

This directory contains **Apache Airflow** DAGs for data orchestration.

## Structure

```
pipelines/
├── dags/                # Airflow DAG definitions
│   └── example_dag.py   # Starter DAG
└── logs/                # Airflow task logs (gitignored)
```
